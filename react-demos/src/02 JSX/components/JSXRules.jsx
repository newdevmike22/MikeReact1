const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <ol>
        <li>
          JSX must return a <strong>single parent element.</strong>
        </li>
        <li>
          JSX elements must be <strong>properly closed.</strong>
        </li>
        <li>
          JSX attributes are written using <strong>camelCase</strong> (e.g., className instead of class).
        </li>
      </ol>
    </div>
  );
};

export default JSXRules;
